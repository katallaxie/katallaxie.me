import React from 'react'
import { GetStaticProps, NextPage } from 'next'
import Document, {
  DocumentContext,
  DocumentProps,
  Head,
  Html,
  Main,
  NextScript
} from 'next/document'

const NotFound: NextPage = (props): JSX.Element => {
  return <></>
}

export default NotFound
